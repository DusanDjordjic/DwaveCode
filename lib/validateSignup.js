export const validateSignup = (
  name,
  email,
  password,
  repeatPassword,
  checked
) => {
  // Check ckechBox
  if (!checked) {
    return "Molimo vas da se složite sa uslovima korišćenja";
  }
};
